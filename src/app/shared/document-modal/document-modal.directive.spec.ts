import { DocumentModalDirective } from './document-modal.directive';

describe('DocumentModalDirective', () => {
  it('should create an instance', () => {
    const directive = new DocumentModalDirective();
    expect(directive).toBeTruthy();
  });
});
