import { PrettifyDataTableColumnPipe } from './prettify-data-table-column.pipe';

describe('PrettifyDataTableColumnPipe', () => {
  it('create an instance', () => {
    const pipe = new PrettifyDataTableColumnPipe();
    expect(pipe).toBeTruthy();
  });
});
